SESSION PROMPT — v0.4

This session has no memory beyond the current interaction.

All context in this file:
- Exists only for the lifetime of the session
- Is reset on browser refresh
- Is not persisted or recalled later

No assumptions should be made about prior messages once the session ends.
